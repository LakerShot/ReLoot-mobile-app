# ReLoot Landing - Обзор Проекта

## 📋 Описание

**ReLoot Landing** - это кросс-платформенное приложение, построенное на базе современного монорепозитория с использованием Tamagui, Solito, Next.js и Expo. Проект позволяет разрабатывать единую кодовую базу для Web (Next.js) и Native (iOS/Android через Expo) платформ.

## 🏗️ Архитектура Монорепозитория

Проект использует **монорепозиторий** с разделением на приложения и переиспользуемые пакеты:

```
ReLoot-landing/
├── apps/                    # Приложения
│   ├── expo/               # Native приложение (iOS/Android)
│   │   ├── app/           # Expo Router
│   │   ├── assets/        # Ресурсы (изображения, шрифты)
│   │   └── scripts/       # Скрипты сборки
│   └── next/              # Web приложение (Next.js)
│       ├── app/           # App Router (новый роутер Next.js)
│       ├── pages/         # Pages Router (старый роутер)
│       ├── public/        # Статические файлы
│       └── __tests__/     # Тесты
│
├── packages/              # Общие пакеты
│   ├── app/              # Основная бизнес-логика
│   │   ├── features/     # Функциональность по фичам
│   │   │   ├── home/    # Домашняя страница
│   │   │   └── user/    # Пользовательская страница
│   │   └── provider/    # Провайдеры (контекст, навигация)
│   ├── ui/               # UI Kit (@my/ui)
│   │   └── src/         # Переиспользуемые компоненты
│   └── config/           # Конфигурация (@my/config)
│       └── src/         # Настройки Tamagui и темы
│
├── scripts/              # Утилиты и скрипты
├── .github/             # GitHub Actions
├── .husky/              # Git hooks
└── node_modules/        # Зависимости
```

## 🛠️ Технологический Стек

### Основные Технологии

| Технология | Версия | Назначение |
|-----------|--------|-----------|
| **Node.js** | 22.x | Runtime окружение |
| **Yarn** | 4.5.0 | Менеджер пакетов |
| **TypeScript** | ~5.8.3 | Типизация |
| **React** | 19.0.0 | UI библиотека |
| **React Native** | 0.79.2 | Native разработка |

### Фреймворки и Инструменты

#### Web Platform
- **Next.js** `14.2.14` - React фреймворк для SSR/SSG
- **React DOM** `19.0.0` - Рендеринг для браузера
- **React Native Web** `~0.20.0` - Адаптер RN компонентов для Web

#### Mobile Platform
- **Expo SDK** - Framework для React Native
- **Expo Router** - Файловая навигация
- **React Navigation** - Навигация

#### UI & Styling
- **Tamagui** `^1.138.6` - Кросс-платформенная UI система с оптимизацией
  - `@tamagui/animations-react-native` - Анимации
  - `@tamagui/lucide-icons` - Иконки
  - `@tamagui/themes` - Темизация
  - `@tamagui/toast` - Уведомления
- **Solito** `^4.2.2` - Унифицированная навигация между Web и Native

#### Development Tools
- **Turbo** `^1.13.4` - Система сборки монорепозитория
- **Vitest** `^2.1.1` - Тестирование
- **Biome** `^1.9.3` - Линтер и форматер
- **Prettier** `^3.3.3` - Форматирование кода
- **Husky** `^9.1.6` - Git hooks

## 🚀 Команды Запуска

### Установка Зависимостей

```bash
# Установка всех зависимостей монорепозитория
yarn install

# или
yarn
```

### Разработка

```bash
# Web разработка (Next.js)
yarn web                    # Запуск dev-сервера
yarn web:extract           # Запуск с оптимизацией (для тестирования)
yarn web:prod              # Production сборка
yarn web:prod:serve        # Запуск production сборки (порт 8151)

# Native разработка (Expo)
yarn native                # Запуск Expo dev-сервера
yarn native:prebuild       # Prebuild для нативных зависимостей
yarn ios                   # Запуск на iOS симуляторе
yarn android               # Запуск на Android эмуляторе
```

### Сборка

```bash
# Сборка всех пакетов (кроме next-app)
yarn build

# Сборка только web приложения
yarn web:prod
```

### Тестирование

```bash
# Запуск тестов (Vitest)
yarn test                  # Разовый запуск
yarn test:watch            # Watch режим
```

### Утилиты

```bash
# Обновление Tamagui
yarn upgrade:tamagui        # Stable версия
yarn upgrade:tamagui:canary # Canary версия

# Проверка Tamagui конфигурации
yarn check-tamagui

# Watch режим для разработки пакетов
yarn watch
```

## 📦 Управление Зависимостями

### Pure JS Зависимости (Cross-platform)

Для зависимостей, которые работают на всех платформах:

```bash
cd packages/app
yarn add [package-name]
cd ../..
yarn
```

**Примеры:** `date-fns`, `lodash`, `axios`

### Native Зависимости (React Native)

Для библиотек с нативным кодом:

```bash
cd apps/expo
yarn add [package-name]
cd ../..
yarn
```

**Примеры:** `react-native-reanimated`, `@react-native-async-storage/async-storage`

### Web-only Зависимости

Для зависимостей только для Next.js:

```bash
cd apps/next
yarn add [package-name]
cd ../..
yarn
```

### ⚠️ Важно

При установке нативных библиотек в `packages/app` для автоимпорта:
- Устанавливайте **ТОЧНО** ту же версию в `apps/expo`
- Используйте `yarn upgrade-interactive` для синхронизации версий
- Несовпадение версий может вызвать серьезные баги

## 🎨 UI Kit (@my/ui)

Проект использует собственную систему дизайна на базе Tamagui:

```typescript
// packages/ui/src/index.tsx
export * from './MyComponent'
export * from './Button'
// ... другие компоненты
```

### Создание Нового Компонента

1. Создайте файл в `packages/ui/src/YourComponent.tsx`
2. Экспортируйте компонент из `packages/ui/src/index.tsx`
3. Используйте в приложениях:

```typescript
import { YourComponent } from '@my/ui'
```

## 🧪 Тестирование

Проект использует **Vitest** для тестирования:

```typescript
// apps/next/__tests__/example.test.ts
import { describe, it, expect } from 'vitest'

describe('Example Test', () => {
  it('should work', () => {
    expect(true).toBe(true)
  })
})
```

## 🌐 Деплой

### Vercel (Next.js)

**Настройки проекта:**
- **Root Directory:** `apps/next`
- **Install Command:** `yarn set version stable && yarn install`
- **Build Command:** (оставить по умолчанию)
- **Output Directory:** (оставить по умолчанию)

### Expo (Mobile)

```bash
# Использовать EAS Build для production сборок
cd apps/expo
npx eas-cli build --platform all
```

## 🔧 Конфигурация

### TypeScript

- `tsconfig.base.json` - Базовая конфигурация
- `tsconfig.json` - Корневая конфигурация
- Каждый пакет/приложение имеет свой `tsconfig.json`, наследующий базовую конфигурацию

### Turbo

`turbo.json` - конфигурация системы сборки:
- Управление кешированием
- Определение порядка сборки пакетов
- Параллельное выполнение задач

### Biome

`biome.json` - настройки линтера и форматера:
- Правила линтинга
- Стиль кодирования
- Игнорируемые файлы

## 🎯 Best Practices

### Организация Кода

1. **Features First** - организуйте код по фичам, а не по типам файлов:
   ```
   packages/app/features/
   ├── home/
   │   ├── screen.tsx
   │   ├── hooks.ts
   │   └── types.ts
   └── user/
       └── screen.tsx
   ```

2. **Shared Components** - переиспользуемые компоненты в `packages/ui`

3. **Business Logic** - бизнес-логика в `packages/app`

### Навигация

Используйте **Solito** для унифицированной навигации:

```typescript
import { Link } from 'solito/link'

<Link href="/user/123">
  <Text>Go to User</Text>
</Link>
```

### Стилизация

Используйте **Tamagui** для кросс-платформенных стилей:

```typescript
import { YStack, Text } from 'tamagui'

export function MyComponent() {
  return (
    <YStack padding="$4" backgroundColor="$background">
      <Text fontSize="$6" color="$color">
        Hello World
      </Text>
    </YStack>
  )
}
```

## 📱 Режимы Роутинга Next.js

Проект поддерживает оба режима:
- **App Router** (новый, рекомендуется) - `apps/next/app/`
- **Pages Router** (legacy) - `apps/next/pages/`

Для переключения на Pages Router следуйте инструкциям в README.md.

## 🐛 Отладка

### Next.js Compiler

Добавьте комментарий `// debug` в начало файла для вывода отладочной информации компилятора Tamagui.

### Логирование

```typescript
console.log('[Feature]:', data) // Используйте префиксы для удобства
```

## 🔗 Полезные Ссылки

- [Tamagui Documentation](https://tamagui.dev)
- [Solito Documentation](https://solito.dev)
- [Next.js Documentation](https://nextjs.org/docs)
- [Expo Documentation](https://docs.expo.dev)
- [React Native Documentation](https://reactnative.dev)

## 🤝 Contributing

1. Создайте feature ветку
2. Внесите изменения
3. Убедитесь, что тесты проходят: `yarn test`
4. Проверьте форматирование: `yarn biome check .`
5. Создайте Pull Request

## 📄 Лицензия

Private project

---

**Создано:** 2025-12-04  
**Node.js:** v22.14.0  
**Yarn:** 4.5.0  
**Статус:** ✅ Зависимости установлены
